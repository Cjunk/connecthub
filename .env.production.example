# Production environment overrides
# Copy to .env.production on the server (keep out of git)

APP_ENV=production
APP_DEBUG=false

# URLs
BASE_URL=https://www.phat-fitness.com
SITE_URL=https://www.phat-fitness.com

# Database
DB_HOST=localhost
DB_NAME=uhura_dbase
DB_USER=jerichosharman
DB_PASS=REPLACE_WITH_STRONG_PASSWORD
DB_PORT=3306

# Email (optional)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# FROM_EMAIL=noreply@your-domain.com
